# Studio V2 · 发布前检查

日期：2026-09-24。范围为当前源码、根路径与 `/new-web-leo/` 子路径生产构建，以及本机浏览器的主要使用流程。检查范围内未发现剩余的阻塞性问题；不等于所有设备、第三方服务或正式托管均已验证。

## 本轮修复

- 图片查看器、便签删除确认和手机便签抽屉打开时，阻止桌面搜索快捷键抢占焦点，避免隐藏 APP 的 Portal 遮罩残留。新增真实 APP 集成回归测试。
- Projects 关闭重开后，窗口栏目标题随初始页面复位，不再残留之前的栏目。
- 工地介绍与测试更新为九阶段、270 秒周期、扩建区域及最终交付状态。
- 移除网站内过期的“尚未推送”状态文案；源码地址与网站预览地址明确区分。
- 精简公开 README，清理文档中的本机路径及内部工作日志链接。可选历史图标生成器不再带默认服务端点，需显式配置 HTTPS 地址。

## 自动化结果

| 检查 | 结果 |
| --- | --- |
| Vitest 全量 | 28 个测试文件、451 项通过 |
| 应用与构建配置 TypeScript | 通过 |
| E2E TypeScript 静态检查 | 通过 |
| ESLint | 0 errors；7 条既有 shared UI Fast Refresh warnings |
| `git diff --check` | 通过 |
| `npm audit --omit=dev` | 本次查询未报告已知生产依赖漏洞 |
| 根路径、子路径 Vite 构建 | 均通过，无构建警告 |
| 两套静态资源检查 | 各 275 项、210 manifest 条目、61 public 文件，0 errors |
| 原图保留 | 45 张原图与源码逐字节一致，未恢复已移除的个人照片 |
| HTTP 冒烟 | HEAD/MIME 正常；音频 Range 返回 206 和所请求的 32 字节 |

两套构建与 public 中的沙盘 HTML 相同：654,833 字节，SHA-256 为 `11588146753476f3d10b8531ce11b582a938d49b08d36b1e4bb86ed74b6b6ad5`。

生产依赖审计不是完整安全审计。公开候选文本未检出常见凭据、私钥、本机绝对路径或内部服务地址；未逐项审计 Git 历史、二进制元数据与素材版权。

## 浏览器实测

- 个人介绍、背景与联系页面：内容、窗口关闭和跨 APP 跳转正常。
- 相册：45 项目录、中文搜索、列表切换、原图加载与前后切图正常。
- 摄影、日记、电影、风味：主题/月份/类型筛选、空态恢复、随机菜品正常。
- 音乐：本地背景音实际播放、时间轴推进；收起后可作为背景音继续，关闭窗口后音频元素卸载。未把播放进度当作听感验证。
- 便签：新建专用测试便签，中文编辑、重开保存、软删除和恢复通过；删除确认内快捷键不会打开桌面搜索。已有便签未修改，测试便签最后移入可恢复的最近删除，未执行永久删除。
- 牧猫庭院：使用已有存档续局、移动和暂停；离开 APP 自动暂停，再次打开提示继续；手机与桌面布局正常。未清空存档或冒充完成所有关卡实测。
- Projects：小作品、创作手记、四个观察栏目与来源说明可访问；关闭重开标题复位。
- 沙盘：九阶段逐个定位成功，100% 显示完整写字楼、落地窗和入口；交付时作业人数及设备读数归零。暂停状态在收起/重开间保留，关闭后 iframe 数量为 0。
- 响应式：实际检查默认窄屏、390×844 与 1440×900 下的关键内容。当前设备交付场景设置面板曾显示约 60 FPS，不作为跨设备性能承诺。

重建生产文件期间，旧页面曾因旧 chunk 已被替换而出现加载恢复提示；刷新到最终构建后相关 APP 正常。正式部署宜采用原子发布，并考虑保留旧的哈希资源以服务已打开的页面。

## 验证边界与部署事项

- 未执行独立 Playwright CLI E2E 套件；E2E 用例仅完成规格更新、静态检查，实际 UI 由浏览器逐项验收。
- 外部 Unity、在线音乐试听及外部内容的持续可用性不在保证范围。
- 下载文件实际落盘、直接 `file://` 运行独立沙盘，本轮未验证；HTTP 渲染与无外部依赖检查不能代替它们。
- 未执行 Lighthouse 或全设备真机测试。
- 仓库查询未发现 Actions workflows 或 deployments 记录，Pages 状态未确认。GitHub 推送不代表网站已上线，也不能据此排除其他托管平台的自动部署。
- 正式域名确定后需补充 canonical、`og:url` 与绝对 HTTPS 分享图；配置旧路径 SPA 回退、HTTPS 和缓存。缺失静态资源应返回 404，不能照搬 Vite preview 的 HTML 回退。

本次检查前已保留完整源码归档 `studio-v2-before-main-audit-20260924-113341.tar.gz`，SHA-256 为 `53d2e16d4fb52b51bb4b04d23b6fc74b269d9194ac876be074d797ca8586e73d`。本地内部日志不提交，原版及 Git 历史保留，不使用强推。

此记录描述提交前的验证结果；远端更新以 GitHub `main` 的提交历史为准。
