# 网站交付与验证

## v14 全站审查与依赖加固

- 全量审查桌面应用、数据、本地状态、路由、沙盘、构建与发布配置。
- React Router 升级至 7.18.3，Vite 升级至 7.3.6，Vitest 升级至 4.1.11，
  PostCSS 升级至 8.5.28。
- 修复升级后的 Router 参数和 ES2020 类型兼容问题。
- 修复微信号复制成功提示不会自动复位的问题。
- 重新生成并通过 `npm ci` 验证锁文件。
- `npm audit` 从 26 条公告降为 0。

最终验证：TypeScript、生产构建通过；ESLint 0 错误、7 条既有警告；
单元测试 11 通过；Playwright 37 通过、3 个按设计跳过、0 失败。
完整记录见 `FINAL-AUDIT-V14.md`。

## v13 车辆连续性

- 删除渣土车、搅拌车和装载机由工序强度直接控制的硬隐藏逻辑。
- 三辆环线车全程保持可见，按 `working`、`returning`、`standby`
  三态执行作业、空载回场和等待。
- 车队共享路线时钟并保留 40 秒间隔，工序变化不再重置车辆坐标。
- 装载机非作业时平滑回到固定停车位，不再消失后重新出现。
- 所有可见车辆均参与边界、灯杆、车距和装载机净距检查。

验证结果：120 秒完整路线最小中心间距 `11.6`；四个关键工序、自动连续采样
和新工期转场均保持 3 辆环线车及装载机可见；碰撞违规为 0；单元测试
11 通过；Playwright 35 通过、3 个按设计跳过、0 失败。实现细节见
`VEHICLE-CONTINUITY-V13.md`。

## v12 1.5 倍场地与分区 LOD

- 工地基座由 `22.5×16.5` 扩展为 `27.5×20.25`，面积从 `371.25`
  增加到 `556.875`，倍率精确为 `1.5`。
- 核心楼、基坑和车辆环线保持原坐标；围挡、入口、桌面、雨尘范围和外围岗位
  随新边界扩展。
- 新增西侧后勤、北侧管线、东侧预制三个场景区，并与基坑、主体结构组成
  五个错峰施工分区。
- 三个远区各包含高低两套实例模型。全景和手机只渲染 11 个低模实例，
  街道近景自动切换两个高模区并渲染 69 个远区实例。
- LOD 阈值带 `1.5` 单位滞回，避免镜头位于边界时闪烁。

最终实测：桌面 59–60FPS，手机 59FPS；六阶段碰撞违规为 0；
单元测试 10 通过；Playwright 35 通过、3 个按设计跳过、0 失败。
实现细节见 `EXPANDED-SITE-V12.md`。

## v11 穿模修复

用户截图中的三类穿模已分别修复：

- 装载机路线停在土堆边缘，只允许铲斗接触，不再让车身进入料堆。
- 四根照明杆移出卡车转弯包络；车辆完整 120 秒循环以 `0.01s` 步长采样，
  最小灯杆净距为 `0.3726`。
- 板房内的工人出生点移到门前，巡检路线改为沿正面横向移动；
  六个工序的板房侵入人数均为 0。

新增 2 条静态净距单元测试，当前单元测试为 10 通过；全站 Playwright
仍为 35 通过、3 个按设计跳过、0 失败。实现细节见
`COLLISION-CLEARANCE-V11.md`。

## v10 工人步态与外围岗位修复

- 腿和鞋从侧向 `Z` 轴摆动改为围绕髋部的 `X` 轴前后摆动，鞋与小腿保持同轴，
  到达路线端点时双腿回正。
- 巡逻手臂只在行走段与腿反向摆动，不再左右甩动。
- 原先在外围排成一行的 8 人已分散到北门、西侧、南侧和东侧安全通道，
  分别承担门岗、车辆引导、交通指挥、围挡维护、水电检查、材料运输和道路巡查。
- 新增响应式“人员”镜头，桌面用于近距离检查动作，手机使用更远机位避免裁切。

验证结果：

| 检查 | 结果 |
| --- | --- |
| 腿部运动轴 | 前后摆角约 0.34rad；六阶段侧向摆角均为 0 |
| 外围岗位 | 8 人、8 个岗位、覆盖四边，不再同线站桩 |
| 工序与碰撞 | 六阶段通过，碰撞违规均为 0 |
| 性能 | 桌面 59–60FPS，手机 59FPS |
| 全站回归 | 35 通过、3 个按设计跳过、0 失败 |

实现与证据见 `WORKER-POSTURE-V10.md` 和 `artifacts/worker-posture-v10/`。

## v9 Living Site 施工循环

Little Works 已从同时重复动作的静态工地改为约 210 秒的施工演示：

- 黎明、正午、黄昏、夜晚使用四组独立天空、光照、曝光和工地灯参数。
- 396 个楼体实例按基础、柱梁、楼板、屋面、围护和拆架顺序生成。
- 23 名黄帽普工和 5 名白帽监理按阶段进场，执行钢筋、工具、搬运、
  塔吊指挥和巡检任务；往返任务包含行走、到位、作业、返回和取料停靠。
- 挖机、渣土车、搅拌车、塔吊、装载机均接入同一施工进度；
  装载机承担基坑整平和收尾清障，非作业阶段隐藏。
- 完工后人员和设备先退场，再于扬尘遮罩下回收楼体并进入下一工期。
- HUD 逐帧同步时刻、工序、进度、在岗人数和活动设备数。

本轮真实回扫修复了三项问题：隐藏楼板曾缩成悬浮薄片、地面巡检班组在周期
开头未立即出现、转场中设备曾过早恢复。两次阶段测试失败日志、最终阶段截图、
指标和全量回归日志均保存在 `artifacts/living-site-v9/`；后续成功运行会按
Playwright 默认行为清理先前失败用例目录，因此不将已清理的旧 Trace 列为交付证据。

最终验证：

| 检查 | 结果 |
| --- | --- |
| TypeScript | 通过 |
| ESLint | 0 错误，7 条既有 Fast Refresh 警告 |
| 单元测试 | 8 通过 |
| Playwright | 35 通过、3 个按设计跳过、0 失败 |
| 工序矩阵 | 六阶段永久结构完成度单调增长，最终为 100% |
| 人员角色 | 23 名普工、5 名监理；黄帽/白帽 |
| 碰撞 | 六阶段与转场采样均为 0 次违规 |
| 离线与画布 | 0 个外部请求；桌面和手机 Canvas 像素审计均非空 |
| 性能采样 | 桌面 59–60FPS，手机 59FPS |

实现与验收细节见 `LIVING-SITE-DEMO-V9.md`。

## v8 图标视觉尺寸与工人路径修复

图标现在按源图片的 alpha 可见边界分别缩放，不再只比较外层 DOM 尺寸；
14 个图标的可见主尺寸均为 74–76px，且没有统一白色外框或残留内阴影。
沙盘巡逻与搬运工改为直线路径往返，不再使用椭圆轨迹。详见 `ICON-WORKERS-V8.md`。

## v7 图标与沙盘视觉更新

桌面图标已去除统一白色外框，改为 Dock 风格的素材自身圆角与阴影。
沙盘完成电影级轻量升级，并让 28 名工人分别执行绑钢筋、操作工具、
指挥塔吊、巡逻和搬运任务。详见 `SANDBOX-V7.md`。

## v6 桌面与沙盘体验更新

Music 已移除 Local 音轨；桌面图标统一形状并支持拖动持久化；
Resume 与 Field Notes 图标不再重复；沙盘车辆路线和轮轴运动已重新约束。
沙盘同时加入轻量电影级材质、接触阴影、暗角和三组镜头预设。
最终回归为 33 通过、3 个按设计跳过、0 失败。详见 `DESKTOP-V6.md`。

## v4 原版对齐更新

- 桌面伙伴恢复原版 WebNeko 形态：32×32 像素猫、八方向移动、抓挠与睡眠动画。
- Music 去掉右侧榜单栏，恢复原版单 iPod 构图；轮盘左右按钮切换 10 首榜单音轨。
- 游戏替换为原版公开站实际使用的 `Herding Cats` Unity WebGL 入口，桌面窗口为 700×560。
- 沙盘轮胎与轮毂组成车轴组，只绕横向轴滚动；停车时轮胎停止，车身转弯独立改变航向。
- 最终 Playwright 结果为 32 通过、2 个按设计跳过、0 失败；单元测试为 8 通过。

原版对照、外部游戏边界和车轮运行证据见 `REFERENCE-V4.md` 与
`artifacts/reference-v4/`。
以下 v3/v2 章节保留为历史记录；涉及 Orbit Breaker 的描述不再代表当前游戏入口。

## v3 交付更新

本轮修复桌面小猫的实际运动，拆分 Daybook、Photography、Cinema、Table Stories
四种独立内容体验；Music 增加带来源的 Billboard 2024 年终前十官方试听；
Little Companion 重建为可离线下载的 Three.js r160 体素建筑工地沙盘。
实现和实测边界详见 `SANDBOX-V3.md`。

v3 最终检查：TypeScript、生产构建通过；ESLint 0 错误、7 条既有警告；
14 个单元测试通过；Playwright 桌面/手机 32 通过、2 个按设计跳过、0 失败。
沙盘在断网 `file://` 下无外部请求，桌面与手机采样 59FPS。

## Final Verdict

已完成本地可运行的网站：以新仓库 `new-web-leo` 为基线，恢复 Macxfolio 的桌面视觉结构，
重新实现桌面应用交互，接入旧站全部 49 张图片。没有更改旧站、提交、推送或发布。
交互修订版进一步对齐参考的单应用浮层、空白/Escape 退出、手机全屏，
桌面图标不再使用个人照片；v2 曾新增 Orbit Breaker，已在 v4 替换。

## 视觉参照及边界

用户提供的 Framer 工程预览链接会跳转登录，本轮未访问到该私有副本内部的定制内容。
实际核对的是同名公开演示：`https://macxfolio.framer.website/`。

- 已保存 1440×900 桌面、390×844 手机、Profile、项目窗口、iPod 参考截图。
- 保留参考的蓝色全屏壁纸、40px 菜单栏、散落式桌面图标和五图标玻璃 Dock。
- 桌面图标约 76px，Dock 图标约 60px；普通窗口约 792×630px，白色标题栏和交通灯按钮。
- Profile 使用参考的照片与简介并列、下方能力列表；iPod 约 200px 宽，带滚轮按钮。
- App 名称、图片、文字和具体功能属于 Haonan 的个人网站，不复制模板里的虚构客户资料。
- 相册采用更大的 Finder 窗口、分类侧栏和照片网格，触屏版本改为横向分类导航。
- 不是对整个页面做像素级相等断言：内容已按请求替换，字体栅格化、时钟和响应式尺寸也有差异。
- 私有 Framer 副本的专属修改未核对，不能宣称已与其逐像素一致。

参考截图和新站截图分别位于 `artifacts/reference/` 与 `artifacts/screenshots/`。
本轮专门的交互实测与截图位于 `artifacts/interaction-v2/reference/`、
`artifacts/interaction-v2/screenshots/`，详见 `INTERACTION-V2.md`。

## 图片迁移

| 分类 | 数量 | 桌面入口 |
| --- | ---: | --- |
| 日常 | 7 | Daybook / Personal Atlas |
| 摄影 | 13 | Photography / Personal Atlas |
| 歌手 | 7 | Music 的音乐收藏 / Personal Atlas |
| 电影 | 6 | Cinema |
| 游戏 | 4 | Personal Atlas；v4 游戏入口为 Herding Cats |
| 美食 | 10 | Table Stories |
| 头像 | 1 | Profile / Connect |
| 微信二维码 | 1 | Connect |
| 合计 | 49 | 全部接入 |

这些文件已存在于新仓库，逐字节与旧站一致，故复用而非重复复制。
`artifacts/media-migration.json` 包含每张图的 SHA-256。
影片、游戏和歌手图作为收藏封面，不声称是用户拍摄的摄影作品。
原始图片不改动，展示缩略图不影响原图下载。

## 已完成交互

- 单个当前应用浮层、关闭、收起、恢复、标题栏拖动、桌面空白或 Escape 退出。
- 同一应用不会重复实例化；收起保留内容，切换不会叠放多个窗口；手机全屏，项目页底部左右切换。
- 不再模拟参考里没有的边缘缩放、绿色最大化和黄色 Dock 缩略图。
- v2 历史实现 Orbit Breaker：三关、Matter.js 碰撞、连击、生命和最高纪录；v4 已移除。
- 相册分类、搜索、无结果状态、网格/列表、收藏、全屏预览、左右切换、下载、焦点恢复。
- iPod 使用现有 Lo-fi 音轨和十首官方试听，真实播放/暂停、切歌、时间进度、音量调节，不自动播放。
- Little Works 提供程序化工地、机械车辆、昼夜、雨尘、相机和实体旋钮控制，并可下载为单 HTML。
- 本地笔记新建、编辑、搜索、导出、删除确认；浏览器存储异常有降级提示。
- 应用搜索、日历、亮度、三种壁纸、桌面伙伴开关。
- 原有九个内容路径映射到桌面应用；独立 404 页面提供返回入口。
- 微信复制拒绝或不可用时，不误报成功，也不记录用户输入。

## 验证结果

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| TypeScript | 通过 | `artifacts/interaction-v2/typecheck.log` |
| ESLint | 0 错误，7 条旧 UI 组件 Fast Refresh 警告 | `artifacts/interaction-v2/lint.log` |
| 单元测试 | 14 通过，包括碰撞和三关通关 | `artifacts/interaction-v2/unit.log` |
| 生产构建浏览器回归 | 28 通过，0 失败、0 跳过 | `artifacts/interaction-v2/e2e-delivery.log` |
| 图片实际加载 | 47 张收藏逐张验证 naturalWidth > 0 | 相册 E2E |
| 控制台异常 | 截图流程未捕获 pageerror | `artifacts/interaction-v2/screenshots/errors.json` |
| 差异空白检查 | `git diff --check` 通过 | 最终命令验证 |
| 图片原件核验 | 49 张一致 | `artifacts/media-migration.json` |

测试环境为 macOS / Node.js 24.10.0，Playwright Chromium。
覆盖 1440×900、390×844 手机模拟，以及 320×568、768×1024、1440×700。
未使用真实 iPhone，也不将 Chromium 手机模拟称为 Safari 真机测试。

第二轮扩展测试发现 700px 高度下窗口与 Dock 重叠 2px，调整工作区边界后整轮通过。
失败截图和 Trace 保留在 `artifacts/regression-second-pass/`，不掩盖历史失败。
最终报告为 `artifacts/playwright-report/index.html`。

构建已将 React、Motion 和应用代码分包；应用入口约 279KB，三个 JS 包 gzip 总计约 190KB。
小游戏额外按需加载约 103KB（gzip 约 34KB），不要求进入首页就加载物理引擎。
壁纸由仓库原引用本地化并压缩，字体同样本地化。保留 Browserslist 数据过旧的非阻塞提示。

## 交付位置

- 新站本地开发：`http://127.0.0.1:5174/`
- 源码：当前 `new-web-leo` 目录，分支 `feat/macxfolio-desktop`
- 生产产物：`dist/`
- 最新源码归档：`deliverables/new-web-leo-source-v14.tar.gz`
- 最新静态部署包：`deliverables/new-web-leo-site-v14.zip`
- 最新独立沙盘：`deliverables/little-works-sandbox-v14.html`
- v1 至 v13 交付包全部保留，不覆盖。
- 新手使用说明：根目录 `README.md`，也包含在部署包中。
最终 SHA-256 写入 `deliverables/SHA256SUMS-v14.txt`。

备份位置和恢复命令见 `BACKUP.md`。部署包需要 HTTP 静态托管，不直接双击 `index.html`。

## Safety Cleanup

- 新旧仓库物理隔离，旧站未因本轮修改而变化；开始前两份归档已校验。
- 测试服务随 Playwright 结束关闭。只保留新站本地开发服务，监听 `127.0.0.1`。
- 调试截图、测试数据与 Trace 放在 Git 忽略的 `artifacts/`，不混入生产部署包。
- 没有添加 API 密钥、后端收件、分析追踪或云数据同步。
- 本地收藏和笔记没有服务端备份，浏览器清理数据会清空，应导出重要笔记。
- 已执行完整 `npm audit`，当前报告为 0 条已知漏洞。
- 源码推送到 `feat/macxfolio-desktop`，未启用 GitHub Pages，也未发布到公网；
  第三方模板资源与媒体公开使用授权仍由站点所有者确认。
